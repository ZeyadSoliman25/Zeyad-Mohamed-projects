/*
 * main.c
 *
 *  Created on: Sep 2, 2023
 *      Author: Zeyad Mohamed
 */

#include <avr/io.h>

void Timer0_compare(void){
	TCCR0 = (1<<FOC0) | (1<<WGM01) | (1<<CS00) | (1<<CS02);
	TCNT0 = 0;
	TIMSK |= (1<<OCIE0);
	SREG |= (1<<7);
}
